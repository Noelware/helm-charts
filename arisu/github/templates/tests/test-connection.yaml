apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "github.fullname" . }}-test-connection"
  labels:
    {{- include "github.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "github.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
