apiVersion: v1
kind: Service
metadata:
  name: {{ include "tsubaki.fullname" . }}
  labels:
    {{- include "tsubaki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tsubaki.selectorLabels" . | nindent 4 }}
