apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsubaki.fullname" . }}-test-connection"
  labels:
    {{- include "tsubaki.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tsubaki.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
