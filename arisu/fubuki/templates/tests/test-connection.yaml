apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fubuki.fullname" . }}-test-connection"
  labels:
    {{- include "fubuki.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fubuki.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
